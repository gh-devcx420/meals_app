import 'package:flutter/material.dart';

List<Color> kAppColourList = [
  Colors.purpleAccent,
  Colors.blueAccent,
  Colors.green,
  Colors.yellow,
  Colors.red,
  Colors.orangeAccent,
];

const double kUniversalMealAppRoundness = 12.0;
const EdgeInsets kUniversalScaffoldBodyPadding =
    EdgeInsets.symmetric(horizontal: 8, vertical: 8);

const EdgeInsets kUniversalAlertDialogueTitlePadding =
    EdgeInsets.only(left: 16, right: 16, top: 12, bottom: 9);
const EdgeInsets kUniversalAlertDialogueContentPadding =
    EdgeInsets.only(left: 16, right: 16, top: 9, bottom: 16);

const Color kDefaultCardColour = Colors.orange;

const EdgeInsets kGridItemPadding =
    EdgeInsets.symmetric(horizontal: 12, vertical: 12);

const EdgeInsets kCardMargin = EdgeInsets.symmetric(horizontal: 8, vertical: 8);
const EdgeInsets kTitleContainerPadding =
    EdgeInsets.symmetric(horizontal: 24, vertical: 2);
